# The second specimen of the system for TEG baseline continuous calculations

The content of `scripts/baseline_model.py`, its common logic, the algorithm of its calculations and the construction of TEGs calculated by it are subject to copyright.
The copyright for the content of `baseline_model.py` is confirmed by a certificate (`Certificate.pdf`).
The copyright for the construction of TEGs designed by `baseline_model.py` is confirmed by a patent (`Patent.pdf`).
